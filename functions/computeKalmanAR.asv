function [ A, Cw, K ] = computeKalmanAR(C_phi_zero, C_phi_one, G, sigmae)
    % computing A
    A = C_phi_one * C_phi_zero^-1;
    % computing Cw
    Cw = (eye-A^2)*C_phi_zero;
    
    P = dare(A',G',Q,R)
    K = (S + A*P*G')*(G*P*G' + sigmae*eye)^-1;


end